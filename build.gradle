plugins {
    id 'base'
    id 'weblab.java-conventions'
    id 'weblab.verification'
    id 'weblab.docker'
    id 'weblab.custom'
}

allprojects {
    group = providers.gradleProperty('appGroup').get()
    version = providers.gradleProperty('appVersion').get()
}
