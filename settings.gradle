pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}

rootProject.name = providers.gradleProperty('appName').get()

include providers.gradleProperty('backendDir').get()
include providers.gradleProperty('frontendDir').get()
